<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.vitamincode.vitamincode_be.mapper.StudentsMapper">    <resultMap id="StudentsResultMap" type="com.vitamincode.vitamincode_be.entity.Students">        <id column="student_id" property="studentId"/>        <result column="student_name" property="studentName"/>        <result column="student_email" property="studentEmail"/>        <result column="class_id" property="classId"/>    </resultMap>    <select id="findAllStudents" resultMap="StudentsResultMap">        SELECT student_id, student_name, student_email, class_id        FROM students    </select>    <select id="findStudentById" resultMap="StudentsResultMap">        SELECT student_id, student_name, student_email, class_id        FROM students        WHERE student_id = #{studentId}    </select>    <insert id="insertStudent" parameterType="com.vitamincode.vitamincode_be.entity.Students">        INSERT INTO students (student_name, student_email, class_id)        VALUES (#{studentName}, #{studentEmail}, #{classId})    </insert>    <update id="updateStudent" parameterType="com.vitamincode.vitamincode_be.entity.Students">        UPDATE students        SET student_name  = #{studentName},            student_email = #{studentEmail},            class_id      = #{classId}        WHERE student_id = #{studentId};    </update>    <delete id="deleteStudent" parameterType="int">        DELETE        FROM students        WHERE student_id = #{studentId};    </delete></mapper>