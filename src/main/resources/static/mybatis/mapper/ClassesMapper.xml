<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.vitamincode.vitamincode_be.mapper.ClassesMapper">    <resultMap id="ClassesResultMap" type="com.vitamincode.vitamincode_be.entity.Classes">        <id column="class_id" property="classId"/>        <result column="class_name" property="className"/>        <result column="class_description" property="classDescription"/>    </resultMap>    <select id="findAllClasses" resultMap="ClassesResultMap">        SELECT class_id, class_name, class_description        FROM classes    </select>    <select id="findClassByClassName" resultMap="ClassesResultMap">        SELECT class_id, class_name, class_description        FROM classes        WHERE class_name LIKE CONCAT('%', #{className}, '%')    </select>    <select id="findClassById" resultMap="ClassesResultMap">        SELECT class_id, class_name, class_description        FROM classes        WHERE class_id = #{classId}    </select>    <insert id="insertClass">        INSERT INTO classes (class_name, class_description)        VALUES (#{classEntity.className}, #{classEntity.classDescription});    </insert>    <update id="updateClass">        UPDATE classes        SET class_name        = #{classEntity.className},            class_description = #{classEntity.classDescription}        WHERE class_id = #{classEntity.classId};    </update>    <delete id="deleteClass">        DELETE        FROM classes        WHERE class_id = #{classId};    </delete></mapper>